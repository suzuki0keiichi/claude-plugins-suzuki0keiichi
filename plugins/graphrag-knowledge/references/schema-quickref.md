# Schema Quick-Reference — Router

Read the appropriate reference for the vault type. Do NOT read both at once.

1. **Run `inspect` to check vault type** (`VAULT.md` `schema` field)
2. Read the matching file:

| `schema` in VAULT.md | Reference |
|---|---|
| omitted / `system` | `$REF/schema-quickref-system.md` |
| `project` | `$REF/schema-quickref-project.md` |
