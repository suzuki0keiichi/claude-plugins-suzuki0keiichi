{
  "name": "graphrag-knowledge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GraphRAG knowledge base: Obsidian vault as the single source of truth, with LLM-safe read/write through the shared bin/graphrag.mjs launcher.",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test graphrag/*.test.ts hooks/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
